# Skills

The following skills extend your capabilities.

**Translatable skills** have their description starting with `Invoke with JSON:`. For these, emit the JSON invocation block shown in the description — the system executes it automatically. You do NOT need to read the SKILL.md file first.

**Regular skills**: read the SKILL.md file using the read_file tool, then follow the instructions inside.

Unavailable skills need dependencies installed first.

{{ skills_summary }}
