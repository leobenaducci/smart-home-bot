# Torch is not pinned to a CUDA wheel index here: the base image already
# carries the CUDA 12.4 runtime, and the default PyPI torch wheels are built
# against it.
torch==2.5.1
transformers>=4.57.0
accelerate>=1.0.0
numpy==1.26.4
fastapi==0.115.6
uvicorn[standard]==0.34.0
