fastapi==0.115.6
uvicorn[standard]==0.34.0
aiohttp==3.12.15
paho-mqtt==2.1.0
python-multipart==0.0.20

# openWakeWord's real runtime dependencies, listed here because the package
# itself is installed with --no-deps in the Dockerfile — see the comment there.
# onnxruntime rather than tflite-runtime: it has wheels for this interpreter and
# this box has cores to spare.
onnxruntime==1.20.1
numpy==1.26.4
scipy==1.14.1
tqdm==4.67.1
requests==2.32.3
scikit-learn==1.5.2
